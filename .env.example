# Backend API (local dev — use .env.local)
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Production (Vercel) uses .env.production with Railway URL

# Optional — only if the frontend talks to Mongo directly (not used by default)
# MONGODB_URI=mongodb://localhost:27017/toysemporium

# Environment
NODE_ENV=development
